part of '../_index.dart';

class ProfileDelta extends StatelessWidget {
  const ProfileDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
