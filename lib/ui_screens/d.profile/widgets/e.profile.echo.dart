part of '../_index.dart';

class ProfileEcho extends StatelessWidget {
  const ProfileEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
