part of '../_index.dart';

class HomeDelta extends StatelessWidget {
  const HomeDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
