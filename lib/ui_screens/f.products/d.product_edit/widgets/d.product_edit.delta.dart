part of '../_index.dart';

class ProductEditDelta extends StatelessWidget {
  const ProductEditDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
