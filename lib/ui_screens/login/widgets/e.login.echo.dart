part of '../_index.dart';

class LoginEcho extends StatelessWidget {
  const LoginEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
