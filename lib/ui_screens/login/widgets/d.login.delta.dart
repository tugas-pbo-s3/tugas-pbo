part of '../_index.dart';

class LoginDelta extends StatelessWidget {
  const LoginDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
