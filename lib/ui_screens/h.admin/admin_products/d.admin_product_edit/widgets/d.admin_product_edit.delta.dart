part of '../_index.dart';

class AdminProductEditDelta extends StatelessWidget {
  const AdminProductEditDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
