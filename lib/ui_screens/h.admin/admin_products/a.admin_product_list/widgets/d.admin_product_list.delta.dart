part of '../_index.dart';

class AdminProductListDelta extends StatelessWidget {
  const AdminProductListDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
