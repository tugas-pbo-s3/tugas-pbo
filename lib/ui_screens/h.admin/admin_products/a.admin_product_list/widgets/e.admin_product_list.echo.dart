part of '../_index.dart';

class AdminProductListEcho extends StatelessWidget {
  const AdminProductListEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
