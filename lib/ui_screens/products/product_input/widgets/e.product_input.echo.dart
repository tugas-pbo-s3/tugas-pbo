part of '../_index.dart';

class ProductInputEcho extends StatelessWidget {
  const ProductInputEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
