part of '../_index.dart';

class ProductDetailDelta extends StatelessWidget {
  const ProductDetailDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
