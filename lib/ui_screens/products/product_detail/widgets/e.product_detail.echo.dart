part of '../_index.dart';

class ProductDetailEcho extends StatelessWidget {
  const ProductDetailEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
