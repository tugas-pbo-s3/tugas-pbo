part of '../_index.dart';

class ProductListDelta extends StatelessWidget {
  const ProductListDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
