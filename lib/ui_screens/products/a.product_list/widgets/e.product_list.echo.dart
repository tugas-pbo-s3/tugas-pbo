part of '../_index.dart';

class ProductListEcho extends StatelessWidget {
  const ProductListEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
