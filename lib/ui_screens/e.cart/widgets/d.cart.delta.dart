part of '../_index.dart';

class CartDelta extends StatelessWidget {
  const CartDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
