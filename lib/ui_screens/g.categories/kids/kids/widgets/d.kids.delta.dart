part of '../_index.dart';

class KidsDelta extends StatelessWidget {
  const KidsDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
