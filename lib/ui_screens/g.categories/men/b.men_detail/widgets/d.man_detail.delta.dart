part of '../_index.dart';

class ManDetailDelta extends StatelessWidget {
  const ManDetailDelta({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
