part of '../_index.dart';

class ManDetailEcho extends StatelessWidget {
  const ManDetailEcho({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
